(function initResizer(){const resizer=document.getElementById('resizer');const main=document.querySelector('main');let isResizing=false;let startX=0;let startWidth=0;const savedWidth=localStorage.getItem('a03_gridWidth');if(savedWidth){document.documentElement.style.setProperty('--grid-width',savedWidth);}
resizer.addEventListener('mousedown',(e)=>{isResizing=true;startX=e.clientX;const computed=getComputedStyle(document.documentElement);const currentWidth=computed.getPropertyValue('--grid-width').trim();startWidth=parseFloat(currentWidth)||48;document.body.style.cursor='col-resize';document.body.style.userSelect='none';});document.addEventListener('mousemove',(e)=>{if(!isResizing)return;const deltaX=e.clientX-startX;const mainWidth=main.offsetWidth;const deltaPercent=(deltaX/mainWidth)*100;let newWidth=startWidth+deltaPercent;newWidth=Math.max(25,Math.min(75,newWidth));document.documentElement.style.setProperty('--grid-width',newWidth+'%');});document.addEventListener('mouseup',()=>{if(isResizing){isResizing=false;document.body.style.cursor='';document.body.style.userSelect='';const currentWidth=getComputedStyle(document.documentElement).getPropertyValue('--grid-width').trim();localStorage.setItem('a03_gridWidth',currentWidth);}});})();const LS_KEY='talktalk_admin_tt';const state={tickets:[],messages:[],ui:{ticketSort:{key:'created_at',dir:'desc'},threadSort:{key:'created_at',dir:'desc'},selectedTicketId:null,selectedMsgId:null,theme:'soft-light',},};function uid(prefix='id'){return(prefix+'_'+
(crypto.randomUUID?crypto.randomUUID():Math.random().toString(36).slice(2,10)));}
function nowISO(){return new Date().toISOString();}
function save(){localStorage.setItem(LS_KEY,JSON.stringify({tickets:state.tickets,messages:state.messages,ui:state.ui,}));}
function load(){const raw=localStorage.getItem(LS_KEY);if(raw){try{const obj=JSON.parse(raw);state.tickets=obj.tickets||[];state.messages=obj.messages||[];state.ui={...state.ui,...(obj.ui||{})};}catch(e){}}else{seed();save();}}
function seed(){const names=['김민수','이서연','박지호','최윤아','정하준','한유진','서지우','오세훈','문다은','강하린','유가온','배다인',];for(let i=0;i<12;i++){const tid=uid('tkt');const mem='mem_'+String(1000+i);const status=i%3===0?'new':i%3===1?'pending':'closed';const title=['비밀상담 요청','불안/우울 관련','대인관계 갈등','직무 번아웃','수면 문제','자녀 양육 고민',][i%6]+'#'+
(i+1);const content='샘플 문의 내용입니다. 핵심 키워드 '+
(i%2?'우울':'불안')+' / 상세 서술 ...';state.tickets.push({ticket_id:tid,sMember_id:mem,author_name:names[i%names.length],title_masked:title,content_enc:content,status,created_at:new Date(Date.now()-i*3600_000*8).toISOString(),post_pwd_hash:'',});const msgCount=i%3===0?0:i%3===1?1:2;for(let k=0;k<msgCount;k++){state.messages.push({message_id:uid('msg'),ticket_id:tid,admin_id:'admin0'+((k%3)+1),role:['admin','counselor','owner'][(i+k)%3],body:`관리자 답변 #${k + 1} — 진행 안내/피드백`,visibility:'private',created_at:new Date(Date.now()-(i*4+k)*1800_000).toISOString(),});}}}
function text(t){return(t??'').toString();}
function contains(hay,needle){return text(hay).toLowerCase().includes(text(needle).toLowerCase());}
function sortBy(arr,key,dir='asc'){return[...arr].sort((a,b)=>{const va=a[key],vb=b[key];if(va===vb)return 0;return(va>vb?1:-1)*(dir==='asc'?1:-1);});}
function hasReply(ticket_id){return state.messages.some((m)=>m.ticket_id===ticket_id);}
const tblTickets=document.querySelector('#tblTickets tbody');const ticketsCount=document.getElementById('ticketsCount');const filterStatus=document.getElementById('filterStatus');const filterHasReply=document.getElementById('filterHasReply');const searchTickets=document.getElementById('searchTickets');function currentTicketFilter(t){if(filterStatus.value&&t.status!==filterStatus.value)return false;const has=hasReply(t.ticket_id);if(filterHasReply.value==='true'&&!has)return false;if(filterHasReply.value==='false'&&has)return false;const q=searchTickets.value.trim();if(q){const pool=[t.author_name,t.title_masked,t.content_enc,t.ticket_id,].join(' || ');if(!contains(pool,q))return false;}
return true;}
function renderTickets(){const{key,dir}=state.ui.ticketSort;const data=sortBy(state.tickets.filter(currentTicketFilter),key,dir);tblTickets.innerHTML='';for(const t of data){const tr=document.createElement('tr');if(t.ticket_id===state.ui.selectedTicketId)
tr.classList.add('selected');tr.dataset.id=t.ticket_id;tr.innerHTML=`
<td><span class="cut" title="${escapeHtml(
  t.title_masked || ''
)}">${escapeHtml(t.title_masked || '')}</span></td>
<td><span class="status ${t.status} cut" title="${t.status}">${
      t.status
    }</span></td>
<td><span class="cut" title="${t.has_admin_reply ? '있음' : '없음'}">${
      t.has_admin_reply ? '있음' : '없음'
    }</span></td>
<td><span class="cut" title="${escapeHtml(
  t.author_name || ''
)}">${escapeHtml(t.author_name || '')}</span></td>
<td><span class="cut" title="${escapeHtml(
  t.author_contact || ''
)}">${escapeHtml(t.author_contact || '')}</span></td>
<td><span class="cut" title="${escapeHtml(t.snsgu || '')}">${escapeHtml(
      t.snsgu || ''
    )}</span></td>
<td><span class="mono cut" title="${t.created_at}">${t.created_at
      .replace('T', ' ')
      .slice(0, 19)}</span></td>
`;tblTickets.appendChild(tr);}
ticketsCount.textContent=`${data.length}건`;}
function selectTicket(id){console.log('🎯 selectTicket 호출됨 - id:',id,'type:',typeof id);state.ui.selectedTicketId=id;state.ui.selectedMsgId=null;save();renderTickets();renderThreads();document.getElementById('btnEditTicket').disabled=!id;document.getElementById('btnDelTicket').disabled=!id;document.getElementById('btnAddMsg').disabled=!id;document.getElementById('btnEditMsg').disabled=true;document.getElementById('btnDelMsg').disabled=true;const t=state.tickets.find((x)=>x.ticket_id===id);document.getElementById('threadsTitleHint').textContent=t?` ${t.title_masked}`:'';console.log('🎯 selectTicket 완료 - 선택된 티켓:',t);}
const tblThreads=document.querySelector('#tblThreads tbody');const threadsCount=document.getElementById('threadsCount');const threadsHint=document.getElementById('threadsHint');const searchThreads=document.getElementById('searchThreads');function currentThreads(){const id=state.ui.selectedTicketId;console.log('🔍 currentThreads - selectedTicketId:',id);console.log('🔍 currentThreads - 전체 messages 개수:',state.messages.length);if(!id)return[];const all=state.messages.filter((m)=>{const match=String(m.ticket_id)===String(id);if(match){console.log('✅ 매칭된 메시지:',m);}
return match;});console.log('🔍 currentThreads - 필터링된 messages 개수:',all.length);const q=searchThreads.value.trim();let filtered=all;if(q){filtered=all.filter((m)=>contains([m.body,m.admin_id,m.role,m.message_id].join(' || '),q));}
const{key,dir}=state.ui.threadSort;return sortBy(filtered,key,dir);}
function renderThreads(){tblThreads.innerHTML='';const rows=currentThreads();threadsHint.textContent=!state.ui.selectedTicketId?'티켓을 선택하면 관련 답변이 여기에 표시됩니다.':rows.length===0?'해당 티켓의 답변이 없습니다.':'';for(const m of rows){const tr=document.createElement('tr');if(m.message_id===state.ui.selectedMsgId)
tr.classList.add('selected');tr.dataset.id=m.message_id;tr.innerHTML=`
<td><span class="mono cut" title="${m.created_at}">${m.created_at
      .replace('T', ' ')
      .slice(0, 19)}</span></td>
<td><span class="mono cut" title="${escapeHtml(m.admin_id)}">${escapeHtml(
      m.admin_id
    )}</span></td>
<td><span class="cut" title="${escapeHtml(m.role || '')}">${escapeHtml(
      m.role || ''
    )}</span></td>
<td><span class="cut" title="${escapeHtml(m.body)}">${escapeHtml(
      m.body
    )}</span></td>
<td><span class="mono cut" title="${m.message_id}">${
      m.message_id
    }</span></td>
`;tblThreads.appendChild(tr);}
threadsCount.textContent=`${rows.length}건`;}
document.querySelectorAll('#tblTickets thead th').forEach((th)=>{th.addEventListener('click',()=>{const key=th.dataset.sort;if(!key)return;const sort=state.ui.ticketSort;sort.dir=sort.key===key&&sort.dir==='asc'?'desc':'asc';sort.key=key;save();renderTickets();});});document.querySelectorAll('#tblThreads thead th').forEach((th)=>{th.addEventListener('click',()=>{const key=th.dataset.sort;if(!key)return;const sort=state.ui.threadSort;sort.dir=sort.key===key&&sort.dir==='asc'?'desc':'asc';sort.key=key;save();renderThreads();});});filterStatus.addEventListener('input',()=>renderTickets());filterHasReply.addEventListener('input',()=>renderTickets());searchTickets.addEventListener('input',()=>renderTickets());searchThreads.addEventListener('input',()=>renderThreads());document.querySelector('#tblTickets tbody').addEventListener('click',(e)=>{const tr=e.target.closest('tr');if(!tr)return;selectTicket(tr.dataset.id);});document.querySelector('#tblThreads tbody').addEventListener('click',(e)=>{const tr=e.target.closest('tr');if(!tr)return;state.ui.selectedMsgId=tr.dataset.id;save();renderThreads();document.getElementById('btnEditMsg').disabled=false;document.getElementById('btnDelMsg').disabled=false;});const dlgTicket=document.getElementById('dlgTicket');const btnAddTicket=document.getElementById('btnAddTicket');const btnEditTicket=document.getElementById('btnEditTicket');const btnDelTicket=document.getElementById('btnDelTicket');const btnSaveTicket=document.getElementById('btnSaveTicket');let editingTicketId=null;btnAddTicket.addEventListener('click',()=>{editingTicketId=null;document.getElementById('dlgTicketTitle').textContent='티켓 추가';setTicketForm(undefined,false);dlgTicket.showModal();});btnEditTicket.addEventListener('click',()=>{const id=state.ui.selectedTicketId;if(!id)return;editingTicketId=id;document.getElementById('dlgTicketTitle').textContent='티켓 수정';const t=state.tickets.find((x)=>x.ticket_id===id);console.log('🔍 티켓 수정 버튼 클릭 - ticket_id:',id);console.log('🔍 찾은 티켓 데이터:',t);console.log('🔍 티켓 content_enc:',t?.content_enc);setTicketForm(t,true);dlgTicket.showModal();});btnDelTicket.addEventListener('click',async()=>{const id=state.ui.selectedTicketId;if(!id)return;if(!confirm('선택한 티켓을 삭제할까요? (관련 답변도 함께 삭제)'))
return;try{console.log('🔄 티켓 삭제 API 호출:',id);const response=await fetch(`/secret/api/v1/tickets/${id}`,{method:'DELETE',});if(!response.ok){const error=await response.json();throw new Error(error.error||'티켓 삭제 실패');}
console.log('✅ 티켓 삭제 완료');alert('티켓이 삭제되었습니다.');state.ui.selectedTicketId=null;await loadFromServer();document.getElementById('btnEditTicket').disabled=true;document.getElementById('btnDelTicket').disabled=true;document.getElementById('btnAddMsg').disabled=true;}catch(error){console.error('❌ 티켓 삭제 오류:',error);alert('삭제 중 오류가 발생했습니다: '+error.message);}});function setTicketForm(t={sMember_id:'',author_name:'',status:'new',title_masked:'',content_enc:'',post_pwd_hash:'',},isEdit=false){console.log('🔍 setTicketForm 호출됨');console.log('🔍 받은 티켓 데이터:',t);console.log('🔍 content_enc 값:',t.content_enc);document.getElementById('t_sMember_id').value=t.sMember_id||'';document.getElementById('t_author_name').value=t.author_name||'';document.getElementById('t_status').value=t.status||'new';document.getElementById('t_title_masked').value=t.title_masked||'';document.getElementById('t_content_enc').value=t.content_enc||'';document.getElementById('t_post_pwd_hash').value=t.post_pwd_hash||'';console.log('🔍 폼에 설정된 content_enc:',document.getElementById('t_content_enc').value);document.getElementById('t_post_pwd_hash_row').style.display=isEdit?'':'none';}
function getTicketForm(isEdit){const obj={sMember_id:document.getElementById('t_sMember_id').value.trim(),author_name:document.getElementById('t_author_name').value.trim(),status:document.getElementById('t_status').value,title_masked:document.getElementById('t_title_masked').value.trim(),content_enc:document.getElementById('t_content_enc').value.trim(),};if(isEdit){obj.post_pwd_hash=document.getElementById('t_post_pwd_hash').value.trim();}
return obj;}
btnSaveTicket.addEventListener('click',async()=>{const isEdit=!!editingTicketId;const v=getTicketForm(isEdit);if(!v.sMember_id||!v.title_masked){alert('회원ID와 제목은 필수입니다.');return;}
try{if(isEdit){console.log('🔄 티켓 수정 API 호출:',editingTicketId);const updateData={title:v.title_masked,content:v.content_enc,author_name:v.author_name,author_contact:v.author_contact||'',status:v.status,post_pwd_hash:v.post_pwd_hash,};console.log('📤 수정 데이터:',updateData);const response=await fetch(`/secret/api/v1/tickets/${editingTicketId}`,{method:'PUT',headers:{'Content-Type':'application/json',},body:JSON.stringify(updateData),});if(!response.ok){const error=await response.json();throw new Error(error.error||'티켓 수정 실패');}
console.log('✅ 티켓 수정 완료');alert('티켓이 수정되었습니다.');}else{console.log('🔄 티켓 추가 API 호출');const createData={title:v.title_masked,content:v.content_enc,post_password:'admin123',author_name:v.author_name,author_contact:v.author_contact||'',snsgu:'A0001',agreement:1,};console.log('📤 추가 데이터:',createData);const response=await fetch('/secret/api/v1/tickets/',{method:'POST',headers:{'Content-Type':'application/json',},body:JSON.stringify(createData),});if(!response.ok){const error=await response.json();throw new Error(error.error||'티켓 추가 실패');}
const result=await response.json();console.log('✅ 티켓 추가 완료:',result);alert('티켓이 추가되었습니다.');}
await loadFromServer();dlgTicket.close();}catch(error){console.error('❌ 티켓 저장 오류:',error);alert('저장 중 오류가 발생했습니다: '+error.message);}});dlgTicket.addEventListener('click',(e)=>{if(e.target.matches('[data-close]'))dlgTicket.close();});const dlgMsg=document.getElementById('dlgMsg');const btnAddMsg=document.getElementById('btnAddMsg');const btnEditMsg=document.getElementById('btnEditMsg');const btnDelMsg=document.getElementById('btnDelMsg');const btnSaveMsg=document.getElementById('btnSaveMsg');let editingMsgId=null;btnAddMsg.addEventListener('click',()=>{const tid=state.ui.selectedTicketId;if(!tid)return;editingMsgId=null;document.getElementById('dlgMsgTitle').textContent='답변 추가';setMsgForm();dlgMsg.showModal();});btnEditMsg.addEventListener('click',()=>{const id=state.ui.selectedMsgId;if(!id)return;editingMsgId=id;document.getElementById('dlgMsgTitle').textContent='답변 수정';const m=state.messages.find((x)=>x.message_id===id);setMsgForm(m);dlgMsg.showModal();});btnDelMsg.addEventListener('click',async()=>{const id=state.ui.selectedMsgId;if(!id)return;if(!confirm('선택한 답변을 삭제할까요?'))return;try{console.log('🔄 메시지 삭제 API 호출:',id);const response=await fetch(`/secret/api/v1/admin/messages/${id}`,{method:'DELETE',});if(!response.ok){const error=await response.json();throw new Error(error.error||'메시지 삭제 실패');}
console.log('✅ 메시지 삭제 완료');alert('답변이 삭제되었습니다.');state.ui.selectedMsgId=null;await loadFromServer();btnEditMsg.disabled=true;btnDelMsg.disabled=true;}catch(error){console.error('❌ 메시지 삭제 오류:',error);alert('삭제 중 오류가 발생했습니다: '+error.message);}});function setMsgForm(m={admin_id:'',role:'ADMIN',visibility:'private',body:''}){document.getElementById('m_admin_id').value=m.admin_id||'';document.getElementById('m_role').value=m.role||'ADMIN';document.getElementById('m_visibility').value=m.visibility||'private';document.getElementById('m_body').value=m.body||'';}
function getMsgForm(){return{admin_id:document.getElementById('m_admin_id').value.trim(),role:document.getElementById('m_role').value,visibility:document.getElementById('m_visibility').value,body:document.getElementById('m_body').value.trim(),};}
btnSaveMsg.addEventListener('click',async()=>{const tid=state.ui.selectedTicketId;if(!tid){alert('티켓을 먼저 선택하세요.');return;}
const v=getMsgForm();if(!v.admin_id||!v.body){alert('관리자ID와 본문은 필수입니다.');return;}
try{if(editingMsgId){console.log('🔄 메시지 수정 API 호출:',editingMsgId);console.log('📤 전송 데이터:',{content:v.body,role:v.role});const response=await fetch(`/secret/api/v1/admin/messages/${editingMsgId}`,{method:'PUT',headers:{'Content-Type':'application/json',},body:JSON.stringify({content:v.body,role:v.role,}),});if(!response.ok){const error=await response.json();throw new Error(error.error||'메시지 수정 실패');}
console.log('✅ 메시지 수정 완료');alert('답변이 수정되었습니다.');}else{console.log('🔄 관리자 답변 추가 API 호출');console.log('📤 전송 데이터:',{content:v.body,admin_id:v.admin_id,role:v.role,});const response=await fetch(`/secret/api/v1/admin/tickets/${tid}/reply`,{method:'POST',headers:{'Content-Type':'application/json',},body:JSON.stringify({content:v.body,admin_id:v.admin_id,role:v.role,}),});if(!response.ok){const error=await response.json();throw new Error(error.error||'답변 추가 실패');}
console.log('✅ 관리자 답변 추가 완료');alert('답변이 추가되었습니다.');}
await loadFromServer();dlgMsg.close();}catch(error){console.error('❌ 메시지 저장 오류:',error);alert('저장 중 오류가 발생했습니다: '+error.message);}});dlgMsg.addEventListener('click',(e)=>{if(e.target.matches('[data-close]'))dlgMsg.close();});window.addEventListener('keydown',(e)=>{if(document.querySelector('dialog[open]'))return;if(e.key==='/'){e.preventDefault();(document.activeElement?.closest('#threadsPanel')?searchThreads:searchTickets).focus();return;}
if(e.key==='Delete'){if(document.activeElement?.closest('#threadsPanel'))
btnDelMsg.click();else btnDelTicket.click();}
if(e.key==='Enter'){if(document.activeElement?.closest('#threadsPanel'))
btnEditMsg.click();else btnEditTicket.click();}
const inThreads=document.activeElement?.closest('#threadsPanel');if(e.key==='ArrowDown'||e.key==='ArrowUp'){e.preventDefault();const tbody=inThreads?tblThreads:tblTickets;const rows=[...tbody.querySelectorAll('tr')];if(rows.length===0)return;const selId=inThreads?state.ui.selectedMsgId:state.ui.selectedTicketId;let idx=rows.findIndex((r)=>r.dataset.id===selId);idx=idx<0?0:e.key==='ArrowDown'?Math.min(idx+1,rows.length-1):Math.max(idx-1,0);const id=rows[idx].dataset.id;if(inThreads){state.ui.selectedMsgId=id;save();renderThreads();btnEditMsg.disabled=false;btnDelMsg.disabled=false;}else{selectTicket(id);}
rows[idx].scrollIntoView({block:'nearest'});}});const btnTheme=document.getElementById('btnTheme');btnTheme.addEventListener('click',()=>{const next=document.body.getAttribute('data-theme')==='soft-light'?'soft-dark':'soft-light';document.body.setAttribute('data-theme',next);state.ui.theme=next;save();btnTheme.textContent='테마: '+
(next==='soft-light'?'부드러운 라이트':'부드러운 다크');});const btnClose=document.getElementById('btnClose');btnClose.addEventListener('click',()=>{if(window.self!==window.top){window.parent.postMessage('closeIframe','*');}else{window.close();}});function escapeHtml(s){return text(s).replace(/[&<>"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;',}[c]));}
load();document.body.setAttribute('data-theme',state.ui.theme||'soft-light');btnTheme.textContent='테마: '+
((state.ui.theme||'soft-light')==='soft-light'?'부드러운 라이트':'부드러운 다크');async function loadFromServer(){try{console.log('🔄 서버 DB에서 데이터 로드 시작...');const ticketsResponse=await fetch('/secret/api/v1/tickets/');if(!ticketsResponse.ok){throw new Error(`Tickets API 오류: ${ticketsResponse.status}`);}
const ticketsData=await ticketsResponse.json();console.log('📦 Tickets 원본 응답:',ticketsData);if(Array.isArray(ticketsData)){state.tickets=ticketsData.map((ticket)=>({ticket_id:ticket.ticket_id||ticket.id,sMember_id:ticket.sMember_id||ticket.smember_id||'',author_name:ticket.author_name||'',author_nickname:ticket.author_nickname||'',author_contact:ticket.author_contact||'',title_masked:ticket.title_masked||ticket.title||'',content_enc:ticket.content_enc||'',status:ticket.status||'OPEN',has_admin_reply:ticket.has_admin_reply||false,created_at:ticket.created_at||new Date().toISOString(),updated_at:ticket.updated_at||null,post_pwd_hash:ticket.post_pwd_hash||'',author_phone:ticket.author_phone||'',author_mobile:ticket.author_mobile||'',author_email:ticket.author_email||'',author_gender:ticket.author_gender||'',birth_year:ticket.birth_year||null,snsgu:ticket.snsgu||'',agreement:ticket.agreement||0,}));console.log('✅ Tickets 로드 완료:',state.tickets.length,'건');console.log('📊 첫 번째 티켓 샘플:',state.tickets[0]);}else{console.warn('⚠️ Tickets 응답이 배열이 아닙니다:',ticketsData);state.tickets=[];}
console.log('🔄 Messages API 호출 시작...');const messagesResponse=await fetch('/secret/api/v1/messages/');console.log('📡 Messages API 응답 상태:',messagesResponse.status);console.log('📡 Messages API 응답 OK:',messagesResponse.ok);if(!messagesResponse.ok){console.error('❌ Messages API 오류 - 상태 코드:',messagesResponse.status);const errorText=await messagesResponse.text();console.error('❌ 오류 내용:',errorText);throw new Error(`Messages API 오류: ${messagesResponse.status}`);}
const messagesData=await messagesResponse.json();console.log('📦 Messages 원본 응답:',messagesData);console.log('📦 Messages 응답 타입:',typeof messagesData);console.log('📦 Messages 배열 여부:',Array.isArray(messagesData));if(Array.isArray(messagesData)){state.messages=messagesData.map((msg)=>({message_id:msg.message_id||msg.msg_id||msg.id,ticket_id:msg.ticket_id,role:msg.role||'admin',content_enc:msg.content_enc||msg.body||'',body:msg.body||msg.content_enc||'',admin_id:msg.admin_id||'',created_at:msg.created_at||new Date().toISOString(),}));console.log('✅ Thread Messages 로드 완료:',state.messages.length,'건');if(state.messages.length>0){console.log('📊 첫 번째 메시지 샘플:',state.messages[0]);}}else{console.warn('⚠️ Messages 응답이 배열이 아닙니다:',messagesData);state.messages=[];}
renderTickets();renderThreads();if(state.tickets.length>0){selectTicket(state.tickets[0].ticket_id);}
save();console.log('✅ 서버 데이터 로드 및 렌더링 완료!');}catch(error){console.error('❌ 서버 데이터 로드 중 오류:',error);console.error('상세 오류:',error.message);console.log('📂 localStorage 데이터 사용');renderTickets();renderThreads();if(state.tickets.length>0){selectTicket(state.tickets[0].ticket_id);}}}
loadFromServer();<!--관리자 로그인 체크-->(function checkAdminLogin(){let attempts=0;const maxAttempts=10;function tryCheckAdmin(){attempts++;if(typeof window.getAdminSession==='function'){const adminSession=window.getAdminSession();if(!adminSession||!adminSession.isLoggedIn||!adminSession.admin_id){console.warn('⚠️ 관리자 로그인 정보가 없습니다.');alert('관리자 로그인이 필요합니다.');window.location.href='/secret/admin_login';return;}
console.log('✅ 관리자 인증 완료:',adminSession.username);}else{if(attempts<maxAttempts){setTimeout(tryCheckAdmin,100);}else{console.error('❌ admin_session.js가 로드되지 않았습니다.');console.error('   스크립트 로드 순서를 확인하세요.');alert('관리자 세션 관리 스크립트를 로드할 수 없습니다.\n\n페이지를 새로고침해주세요.');window.location.href='/secret/admin_login';}}}
if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',tryCheckAdmin);}else{tryCheckAdmin();}})();